ENV=dev
SERVICE_NAME=cache-22-server
HTTP_PORT=8080
DB_URL=postgres://cache22:cache22@localhost:5432/cache22?sslmode=disable
LIBRARY_PATH=./data/library
COVER_DIR=./data/covers
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=
# One-time setup token. Used only to create the first admin account.
# After setup it does nothing. Change it in production.
ADMIN_TOKEN=change-me-in-production
